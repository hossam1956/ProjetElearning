{% extends "base.html.twig" %}

{% block title %}Ajouter question{% endblock %}

{% block body %}
<br>
    <div class="container">
        <div class="col-md-8 mx-auto">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">Ajouter question</h3>
                </div>
                <div class="card-body">
                    {{ form_row(form.reponse, {'attr': {'hidden': 'hidden'}, 'label_attr': {'style': 'display: none;'}}) }}
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>
<br>
{% endblock %}
