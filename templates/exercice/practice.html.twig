{% extends 'base.html.twig' %}

{% block title %}Liste exercices{% endblock %}

{# {% block stylesheets %}
    <style type="text/css">
        body {
            background-color: #336699;
        }
    </style>
{% endblock %} #}

{% block body %}
    <div class="container">
        {# <h1>Exercise : {{ exercice.titre }}</h1>
        <ul>
            {% for question in exercice.getQuestions() %}
                <li>
                    <h2>{{ question.question }}</h2>
                    <ul>
                        {% for choix in question.getChoixReponses() %}
                            <li>{{ choix.choix }}</li>
                        {% endfor %}
                    </ul>
                </li>
            {% endfor %}
        </ul> #}

        <div class="col-md-8 mx-auto">
            <div class="card">
                <div class="card-header">
                    {# <h3 class="card-title">Ajouter l'exercice</h3> #}
                </div>
                <div class="card-body">
                    {# templates/task/new.html.twig #}
                    {{ form(form) }}
                </div>
            </div>
        </div>

        {# <h2>Index</h2>
        <p class="important">
            Welcome on my awesome homepage.
        </p> #}

        {# <div class="row my-5">
            <div class="col-md-8 mx-auto">
            {% for message in app.flashes("success") %}
                <div class="alert alert-success">
                    {{ message }} #}
                    {# {{ app.flashes("success") }} #}
                {# </div>
            {% endfor %} #}
                {# <div class="card">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Titre</th>
                                <th>View</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for exercice in exercices %}
                                <tr> #}
                                    {# <td scope="row">{{ exercice.id }}</td>
                                    <td>{{ exercice.titre }}</td> #}
                                    {# <td>
                                        <a href="{{ path('app_exercice.show', {id : exercice.id}) }}" class="btn btn-primary">
                                            Show exercise <i class="fa-regular fa-eye"></i>
                                        </a>
                                        <a href="{{ path('app_exercice.practice', {id : exercice.id}) }}" class="btn btn-primary">
                                            Practice <i class="<i class="fa-solid fa-book"></i>"></i>
                                        </a> #}
                                        {# {% if is_granted('ROLE_USER') %}
                                            {% if task.user.id == app.user.id %}
                                                <a href="{{ path('task_edit', {id : task.id}) }}" class="btn btn-warning">
                                                    <i class="fa-solid fa-pen-to-square"></i>
                                                </a>
                                                <a href="{{ path('task_delete', {id : task.id}) }}" class="btn btn-danger">
                                                    <i class="fa-solid fa-trash"></i>
                                                </a>
                                            {% endif %}
                                        {% endif %} #}
                                    {# </td>
                                </tr> #}
                            {# {% endfor %} #}
                        {# </tbody>
                    </table>
                    <a href="{{ path('app_exercice.add') }}" class="btn btn-primary">
                        <i class="fa-solid fa-plus"></i>                
                        Add an exercise
                    </a>
                </div>
            </div>
        </div> #}
    </div>
{% endblock %}

{# {% block javascripts %}
    
{% endblock %} #}



